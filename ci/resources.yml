resources:
  - name: docker-create-metrics-data-batch-pr
    type: pull-request
    source:
      repository: dwp/docker-create-metrics-data-batch
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: docker-create-metrics-data-batch
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/docker-create-metrics-data-batch.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h
